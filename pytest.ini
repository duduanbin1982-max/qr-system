[pytest]
testpaths = tests
markers =
    unit: fast tests of isolated policy and parsing behavior
    integration: tests that cross service, repository, database, or HTTP boundaries
    contract: tests that protect API, architecture, deployment, or source contracts
