# 生产排程 Task 0：只读基线盘点与数据预检报告

**盘点日期：** 2026-09-21（Asia/Shanghai）
**范围：** 本地代码库与生产主机 `192.168.1.8` 只读检查
**安全边界：** 未执行数据库写入、迁移、停服、重启、部署、功能开关修改或生产数据补录。

## 1. 代码与生产发布基线

### 本地代码库

- 工作分支：`codex/production-node-workbench-ux-design`
- 报告生成前工作区干净；本次仅新增本地基线报告，未修改既有代码。
- 当前本地 HEAD：`b55e4efa386e9bc4f16c4bc079e6cfbf52ce5fdd`
- 当前分支落后 `github/master` 22 个提交；本次 Task 0 没有推送或合并操作。
- 仓库规模：550 个提交、105 个本地/远端分支，首个提交日期 2026-06-06，最近提交日期 2026-09-20。
- 排程相关代码近期集中在 `modules/routes/schedule.py`、`modules/services/schedule_capacity_service.py`、`modules/repositories/schedule_capacity_repository.py` 和 `modules/domain/production_node_scheduling.py`。

### 生产主机

- 主机：`192.168.1.8`
- 生产目录：`/home/dubin/qr-system`
- 当前代码提交：`f161654c3754e5ee33b0ac62883e7369e97b830f`
- SQLite 数据库：`/home/dubin/qr-system/data/production.db`
- `PRAGMA user_version`：`90`
- 数据库文件在盘点时存在，大小约 84.5 MiB。
- system scope 的 `qr-system.service` 为 `inactive/disabled`；user scope 的同名服务为 `active/enabled`，80/443 端口仍在监听。此次未启动、停止或重启任何服务。

## 2. 生产节点与 Legacy 资源

目标业务模型是 21 个节点：下料 1、铆接 4、焊接 10、抛丸 1、打磨 1、镗孔 2、喷漆 2。

| 工序 | `production_nodes` 活动节点 | `process_production_lines` Legacy 资源 | 目标节点数 | 结论 |
|---|---:|---:|---:|---|
| 下料 | 1 | 1 | 1 | 数量一致 |
| 铆接 | 4 | 4 | 4 | 数量一致 |
| 焊接 | 10 | 10 | 10 | 数量一致 |
| 抛丸 | 1 | 1 | 1 | 数量一致 |
| 打磨 | 3 | 1 | 1 | 多出 2 个节点 |
| 镗孔 | 2 | 2 | 2 | 数量一致 |
| 喷漆 | 2 | 2 | 2 | 数量一致 |
| **合计** | **23** | **21** | **21** | 节点主数据比目标多 2 个 |

节点映射结果：

- 23 个活动 `production_nodes` 中，21 个具有 `legacy_process_line_id` 精确映射。
- 2 个未映射节点是打磨节点：`打磨2线`、`打磨3线`。
- 所有 23 个节点当前均为 `exclusive`、活动状态，全部引用默认日历 `calendar_id=1`。
- 21 条 Legacy `process_production_lines` 的工序分布与目标分布完全一致，且每日容量均为 540 分钟。
- 旧订单级 `production_lines` 仍有 3 条记录；它不应再参与新的节点容量排程，但应继续保留为历史兼容数据。
- `production_node_capabilities` 当前没有任何能力限制记录；系统实际上按“同工序节点全部可互换”处理，尚未配置产品、路线版本或工序版本级能力约束。

### 必须业务确认的节点问题

在目标模型明确为“打磨 1 个节点”的前提下，`打磨2线`、`打磨3线` 不能直接删除或自动停用。需要业务确认它们是：

1. 真实存在但暂不纳入排程；
2. 已撤销/重复导入，应保留审计记录后停用；或
3. 目标节点数量应调整为 3。

在确认前不得进行节点归并、停用或容量迁移。

## 3. 日历、班次与停机

默认日历 `DEFAULT/默认生产日历`：

- 时区：`Asia/Shanghai`
- 工作日：周一至周五
- 早班：08:00–12:00，240 分钟
- 晚班：13:00–18:00，300 分钟
- 每个工作日有效生产时间：540 分钟
- 日历例外：0 条
- 停机记录：0 条，节点绑定停机记录也是 0 条

因此，540 分钟的基础容量已配置，但尚未覆盖节假日、临时调班、节点单独停机、换型或维修窗口。当前容量结果不能视为包含真实停机的承诺产能。

## 4. 路线、工序版本和标准工时

- 路线版本：`published=61`、`superseded=26`、`pending_approval=1`。
- 工序版本：`published=31`。
- 非删除订单共 167 个，全部绑定 `route_version_id`。
- 非删除订单工序共 1,179 道，全部绑定 `process_version_id`。
- 当前待生产/生产中订单：95 个（待生产 67、生产中 28），共 664 道订单工序。
- 路线节点一致性：664/664 道工序均能在路线版本中找到精确的 `route_version_id + process_version_id` 项，且工序版本所属 `process_id` 全部一致。
- 活动标准工时记录：402 条，全部具有精确路线版本和工序版本绑定。

### 标准工时覆盖

664 道待生产/生产中订单工序中：

- 659 道能精确命中有效标准工时；
- 5 道未命中精确标准工时，覆盖率为 `659/664 = 99.25%`。
- 这 5 道全部属于已经登记为 `non_scheduled` 的外协/非排程工序：
  - 26070806：加工分体下端面、加工分体上端面；
  - 26071302：两片焊接静音型外壳钻孔；
  - 26071902：加工分体下端面、加工分体上端面。

因此，**内部可排程工序的精确工时覆盖是 100%**；5 道缺口不应通过补录内部工时解决，而应继续按外协/非排程规则处理。当前 659 道内部工序没有显式 `route_process_execution_policies` 行，策略表只记录上述 5 道 `non_scheduled` 例外；需要在代码/审计口径中明确“未登记策略即 internal”的默认规则，避免把 659 道内部工序误报为未分类。

## 5. 当前排程事实与覆盖情况

### 持久化排程事实

- `order_process_schedules`：111 行。
  - `planned`：60 行；
  - `completed`：51 行；
  - `blocked`：0 行；
  - 111 行均有精确路线/工序版本字段；
  - 50 行具有 `production_node_id`，其余历史完成事实没有节点绑定。
- `schedule_revisions`：123 行。
  - `published`：1 行；
  - `draft`：18 行；
  - `cancelled`：104 行；
  - 当前只有订单 `26062502` 存在已发布排程修订版。
- 当前待生产/生产中 95 个订单中，仅少数订单存在排程行；大量订单尚未形成可发布的完整排程版本。
- 影子排程台账为空：`production_node_shadow_runs/items/segments/allocations` 均为 0 行。虽然代码/环境已具备影子模型表，但生产库尚未留下影子运行证据。

### 节点占用分布

已有排程占用高度集中在少数节点：

- 焊接-01：3,890 分钟；焊接-02：510 分钟；焊接-03/04：各 70 分钟；焊接-05 至 10：0 分钟。
- 喷漆-01：4,730 分钟；喷漆-02：500 分钟。
- 打磨-01：4,720 分钟；打磨2线/打磨3线：0 分钟。
- 铆接仅铆接-01 有 1,240 分钟，其余 3 个节点为 0。
- 镗孔-01：4,240 分钟；镗孔-02：450 分钟。

这表明当前排程事实尚不能证明已经实现同类节点的均衡自动分配；需要在影子排程和正式引擎验收中验证节点均衡、跨天和冲突检测。

## 6. 排程运行故障与兼容性观察

最近排程运行统计：

- `generate`：成功 2 次，失败 101 次；
- `dynamic_replan`：成功 17 次，失败 3 次；
- 失败原因一：`min() iterable argument is empty`，83 次；
- 失败原因二：`NOT NULL constraint failed: order_process_schedule_segments.process_line_id`，21 次。

这两类错误说明：

1. 节点/日历候选为空时，排程器仍有未防护的空集合路径；
2. 节点模型与旧分段事实之间仍有 Legacy `process_line_id` 约束耦合。

在这两个问题修复并完成回归测试前，不建议开启正式节点排程引擎，也不建议把当前排程结果作为生产执行承诺。

兼容性与迁移观察：

- `production_node_compatibility_observations`：8 条，`mismatch=0`。
- `production_node_migration_differences`：14 条历史证据，全部是 `intentionally_unassigned_blocked`，对应没有 Legacy 节点映射的历史排程修订项；没有发现需要立即自动改写的差异。

## 7. 功能开关与服务状态

生产目录 `.env` 中当前记录：

```text
PRODUCTION_NODE_QUERY_ENABLED=true
PRODUCTION_NODE_COMPAT_AUDIT_ENABLED=true
PRODUCTION_NODE_WRITE_ENABLED=true
PRODUCTION_NODE_ENGINE_ENABLED=true
```

但实际 system scope 服务为 `inactive/disabled`，user scope 服务为 `active/enabled`。这说明本机存在服务作用域差异；不能仅根据 `.env` 推断正式引擎正在对外提供服务。必须由运维确认 user service 的实际进程、加载环境和当前流量后，才能判断开关是否真正生效。

## 8. Task 0 结论

### 已满足

1. 路线和订单工序版本绑定完整，未发现根级版本回退问题。
2. 内部可排程工序的标准工时精确覆盖率为 100%。
3. 基础 540 分钟日历已配置，节点均引用同一默认日历。
4. 21 条 Legacy 资源与目标工序节点数量一致，并已有 21 条精确映射。
5. 兼容性观察最近结果为 0 mismatch。

### 未满足/高风险

1. 节点主数据实际有 23 个活动节点，打磨多出 2 个且未映射。
2. 尚无节点能力限制、节点例外日历和停机事实。
3. 95 个活动订单中，大量订单没有完整排程版本；当前仅 1 个已发布排程修订版。
4. 影子排程台账为空，无法用生产证据证明新节点算法与旧结果等价。
5. 排程运行存在 83 次空候选 `min()` 失败和 21 次 Legacy 分段约束失败。
6. system/user 服务作用域状态不一致，且生产目录四个节点开关均为开启状态，需运维确认实际生效范围。

## 9. 建议的下一步（仍需单独授权后执行）

### 必须先修代码

1. 对节点候选为空、日历无可用区间、外协工序和节点能力不匹配统一返回明确的 `blocked` 原因，禁止调用空集合 `min()`。
2. 将节点分段写入与 Legacy `process_line_id` 兼容投影彻底解耦，修复 `process_line_id NOT NULL` 约束失败。
3. 在影子模式下写入独立台账，验证 95 个活动订单、659 道内部工序的节点分配、容量、跨班次和冲突结果。

### 必须业务确认

1. `打磨2线`、`打磨3线` 的业务状态和目标是否仍为打磨 1 节点。
2. 是否需要配置具体产品/路线/工序版本的节点能力限制。
3. 是否需要补录 2026 年节假日、调班和已知停机窗口。

### 可在确认后迁移/补录

1. 对确认停用的多余打磨节点执行带审计的停用/映射修订，不删除历史事实。
2. 补充节点能力和日历例外数据。
3. 运行完整影子排程，确认差异为 0 或有业务批准的差异后，才重新授权正式节点写入和引擎切换。

**Task 0 状态：完成（只读盘点完成；未执行任何生产写操作）。**
