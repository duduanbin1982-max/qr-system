# 生产排程 V091 副本验收报告

**验收日期：** 2026-09-21（Asia/Shanghai）
**生产主机：** `192.168.1.8`
**生产代码基线：** `f161654c3754e5ee33b0ac62883e7369e97b830f`
**生产数据库基线：** V090，`quick_check=ok`，外键违规 0
**安全边界：** 本次未执行生产迁移、写入、停服、重启、部署或功能开关修改。

## 1. 代码与测试

- 修复分支：`codex/production-scheduling-runtime-repair`
- 新增 V091：允许节点原生排程分段的 `process_line_id` 为空；节点或 Legacy 产线至少保留一个来源。
- 普通排程和动态重排在空分段时返回 `blocked / NODE_CALENDAR_UNAVAILABLE`，不再调用空集合 `min()`。
- 已完成工序直接写入 `completed`，不进入节点分配。
- 完整后端测试：`1570 passed`。
- 节点/迁移/影子/工作流/部署契约定向测试：`238 passed`。
- 前端单元测试：`290 passed`。
- 前端 API facade、导入环、Vite 构建：全部通过。
- 开发依赖补充 `tzdata>=2024.1`，避免 Windows/CI 的 `ZoneInfo("Asia/Shanghai")` 环境性假失败。

## 2. 生产只读基线与 V091 副本迁移

通过只读连接确认：

- user service `qr-system.service`：`active`
- 活动订单：95 个
- 生产数据库完整性：`ok`

生产库使用 SQLite 在线备份复制到本地临时副本后，仅在副本执行 V091：

- 迁移执行数：1
- 副本版本：V091
- 副本 `quick_check`：`ok`
- 副本外键违规：0
- 副本迁移证据：`migrate-replica-production-scheduling-runtime-v091-replica-20260921-f161654.json`

## 3. 95 个活动订单独立影子试排

在 V091 副本执行 95 个独立影子运行，影子事实写入独立 `production_node_shadow_*` 台账：

- 订单成功：95/95
- 工序事实：664
- 计划内部工序：395
- 阻断：0
- 数量不守恒：0
- 序列件跨节点拆分：0
- 正式排程 6 张表的行数和摘要指纹：前后一致
- 报工进度与 `work_records` 指纹：未变化

独立影子方案之间出现 2062 个跨订单重叠，仅表示每个订单分别假设从同一时点开始，不能作为全局排程冲突结论；因此另行执行了共享容量的全局试排。

## 4. 95 个活动订单全局共享容量试排

按有效缓急顺序，在第二份可丢弃副本中运行自动排程，保留先排订单的节点占用后再排后续订单：

- 队列订单：95/95
- 订单成功：95/95
- 工序总数：664
- 内部计划工序：395
- 阻断：0
- 节点内部冲突：0
- 与既有正式排程冲突：0
- 数量不守恒：0
- 序列件拆分：0
- 报工进度/工作记录：前后一致
- 副本完整性：通过

全局试排的交期风险：

- 最晚预计结束：`2026-11-02 17:30`
- 超期订单：23 个
- 最大延期：32165 分钟

这表明算法容量分配正确，但现有订单池相对交期存在真实产能缺口，不能把“无冲突”误解为“全部按期”。上线前应由业务确认延期订单及加班/外协策略。

## 5. `打磨2线`、`打磨3线` 结论

副本清查结果：

- 节点 ID：22、23
- 均为 `admin` 于 2026-09-20 手工新增
- 无 `legacy_process_line_id` 映射
- 无能力限制、停机、正式排程、发布修订、分配或任务锁引用
- 仅在影子副本中产生假设性占用

在副本将两节点从 `active` 改为 `inactive`（保留记录，不删除），再次运行全局试排：

- 95/95 订单成功
- 节点冲突 0
- 数量不守恒 0
- 序列件拆分 0
- 报工事实未变化

**建议结论：** 按已确认的“打磨只有 1 个稳定生产节点”模型，将 22、23 作为多余节点受控停用；保留节点记录、原始创建审计事件及全部历史证据，不删除、不改写历史排程事实。生产停用仍需单独授权。

## 6. 当前状态与下一步

本次修复和副本验收已完成，但分支尚未提交、推送、创建 PR，也未对生产执行 V091。下一步顺序：

1. 复核本地 diff、报告和测试证据；
2. 在获得明确授权后 commit、push 并创建 PR；
3. PR 合并后，以新的合并 SHA 重新做生产只读预检；
4. 单独授权后才可执行生产备份、停服、V091 迁移或节点停用。
